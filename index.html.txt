<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exclusive Giveaway 2026</title>
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
    <style>
        :root {
            --primary: #007aff;
            --gold: #d4af37;
            --bg: #f5f5f7;
        }

        body {
            font-family: -apple-system, sans-serif;
            background-color: var(--bg);
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
        }

        .card {
            background: white;
            padding: 2rem;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            width: 90%;
            max-width: 400px;
            transition: all 0.3s ease;
        }

        .hidden { display: none; }

        input {
            width: 80%;
            padding: 12px;
            margin: 15px 0;
            border: 1px solid #ddd;
            border-radius: 10px;
            font-size: 1rem;
        }

        button {
            background: var(--primary);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 10px;
            font-weight: bold;
            cursor: pointer;
            width: 100%;
        }

        .prize-option {
            border: 2px solid #eee;
            padding: 15px;
            margin: 10px 0;
            border-radius: 12px;
            cursor: pointer;
            transition: 0.2s;
        }

        .prize-option:hover { border-color: var(--primary); background: #f0f7ff; }

        h2 { color: #333; margin-top: 0; }
        #final-msg { color: #e63946; font-size: 2rem; }
        .from-rithik { font-size: 1.2rem; color: #555; margin-top: 10px; }
    </style>
</head>
<body>

    <div id="step1" class="card">
        <h2 style="color: var(--gold)">🏆 Special Invitation</h2>
        <p>You've been selected for a private giveaway!</p>
        <input type="text" id="userName" placeholder="Enter your full name">
        <button onclick="goToStep2()">Claim My Prize</button>
    </div>

    <div id="step2" class="card hidden">
        <h2>Choose Your Reward, <span id="display-name"></span>!</h2>
        <div class="prize-option" onclick="finishPrank()">📱 iPhone 17 Pro Max</div>
        <div class="prize-option" onclick="finishPrank()">💻 MacBook Pro M5</div>
        <div class="prize-option" onclick="finishPrank()">🎧 AirPods Max 2</div>
    </div>

    <div id="step3" class="hidden">
        <div class="card">
            <h1 id="final-msg">APRIL FOOL! 🤡</h1>
            <p class="from-rithik">Gotcha! Sending love from <strong>Rithik</strong></p>
            <button onclick="location.reload()" style="background: #333; margin-top: 20px;">I'll get you back!</button>
        </div>
    </div>

    <script>
        function goToStep2() {
            const name = document.getElementById('userName').value;
            if (name.trim() === "") {
                alert("Please enter your name to claim the prize!");
                return;
            }
            document.getElementById('display-name').innerText = name;
            document.getElementById('step1').classList.add('hidden');
            document.getElementById('step2').classList.remove('hidden');
        }

        function finishPrank() {
            document.getElementById('step2').classList.add('hidden');
            document.getElementById('step3').classList.remove('hidden');

            // Epic Confetti Rain
            const duration = 10 * 1000;
            const animationEnd = Date.now() + duration;

            (function frame() {
                const timeLeft = animationEnd - Date.now();
                if (timeLeft <= 0) return;

                confetti({
                    particleCount: 3,
                    angle: 60,
                    spread: 55,
                    origin: { x: 0 },
                    colors: ['#007aff', '#ffcc00', '#e63946']
                });
                confetti({
                    particleCount: 3,
                    angle: 120,
                    spread: 55,
                    origin: { x: 1 },
                    colors: ['#007aff', '#ffcc00', '#e63946']
                });

                requestAnimationFrame(frame);
            }());
        }
    </script>
</body>
</html>